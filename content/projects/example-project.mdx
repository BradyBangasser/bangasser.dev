---
title: "Example project write-up"
date: "2026-05-01"
summary: "Replace this with a real project. Set `repo` to link it to a GitHub repository automatically."
tags: ["cloud", "security"]
status: "active"
repo: "BradyBangasser/example-repo"
featured: true
---

Replace this file with a real write-up. A few notes on the frontmatter:

- `status` is one of `active`, `archived`, or `concept`.
- `repo` (optional) is `owner/name` — set this to a real repository and it
  links directly to GitHub from the project page.
- `externalUrl` (optional) can point to a live deployment or demo.
- `featured: true` surfaces the project on the home page.

Everything below this line is the body of the write-up — describe the
problem, the approach, and anything technically interesting about the
solution.
